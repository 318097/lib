import { handleError } from "./errorHandling";
import {
  getDataFromStorage,
  setDataInStorage,
  getToken,
  hasToken,
} from "./localStorage";

export {
  handleError,
  getDataFromStorage,
  setDataInStorage,
  getToken,
  hasToken,
};
