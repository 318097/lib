const {
  generateSlug,
  copyToClipboard,
  checkScrollAtBottom,
} = require("./utils");
// import { formatDate } from "./date";

module.exports = { generateSlug, copyToClipboard, checkScrollAtBottom };
