const {
  generateSlug,
  copyToClipboard,
  checkScrollAtBottom,
} = require("./utils");
// import { formatDate } from "./date";

const test = () => {};

module.exports = { generateSlug, copyToClipboard, checkScrollAtBottom, test };
