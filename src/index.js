const {
  generateSlug,
  copyToClipboard,
  checkScrollAtBottom,
} = require("./utils/common");
import { formatDate } from "./date";

module.exports = {
  generateSlug,
  copyToClipboard,
  checkScrollAtBottom,
  formatDate,
};
