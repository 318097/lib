import { generateSlug, copyToClipboard, checkScrollAtBottom } from "./common";
import { formatDate } from "./date";

module.exports = {
  generateSlug,
  copyToClipboard,
  checkScrollAtBottom,
  formatDate,
};
